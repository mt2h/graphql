/*const sum = (one, two) => {
    const sum = one + two
    return `the sum is ${sum}`
}*/

/*const subtract = (one, two) => {
    const subtract = one - two
    return `the subtract is ${subtract}`
}*/

const sum = (a, b) => a + b

const subtract = (a, b) => a - b

export { sum as default, subtract }
